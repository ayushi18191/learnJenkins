#!/usr/bin/env groovy
// Some fast steps to inspect the build server. Create a pipeline script job and add this:

node {
   echo "stage 1"
   stage('stage 1') {
   
      echo('inside stage 1')
   }
    stage('stage 2') {
   
      echo('inside stage 2')
   }

}
